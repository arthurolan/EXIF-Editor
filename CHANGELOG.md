# 版本记录

## 待发布

- 批量总容量上限提高到 1 GB；最多 100 张、单张最大 500 MB 不变。接近 1 GB 的真实图片处理和 ZIP 打包仍待实测。
- 入队提示分别统计格式不支持、单张过大、数量超限和总容量超限；可展开查看每个被跳过文件的路径与原因。多个条件同时满足时，每项按上述顺序归入一个原因，避免重复计数。
- 队列显示当前容量 / 1 GB；清空队列同时清除上次的选择报告。

## V2.1.0 — 2026-09-07

本版本在 V2.0 单张编辑基础上增加批处理，版本标签为 `v2.1.0`。

### 功能

- 多选照片或选择文件夹，最多 100 个文件、总计 500 MB，单张最大 500 MB。
- 批量隐私清理（包含设备序列号）、删除 GPS。
- 批量写入作者、版权、关键词、城市、国家/地区；留空字段保持原值。
- 按每张照片原始拍摄时间统一偏移天、小时、分钟。
- 批量填写 GPS 或使用与单张相同的地图选点；海拔和拍摄方向可选。地图由用户主动加载。
- 串行处理、逐项核验、失败原因与失败重试；失败或未通过核验的副本不交付，成功项不受其他文件失败影响。
- 明显进度、当前文件和处理阶段提示、完成提示音；独立副本下载及 ZIP 下载。

### 已确认的限制

- HEIC / HEIF 批量文字仅写入并核验作者、版权；关键词、城市、国家/地区保持原值，结果明确提示未写入，已验证副本仍可交付。
- TIFF、HEIC / HEIF 继续保持实验性支持，具体容器和核验限制见 README 的格式表；本次发布没有扩大这些格式的保证范围。
- 缺少或无法解析拍摄时间的文件不能执行时间偏移，会单独失败。
- 地图加载需要联网；照片处理仍在本地。提示音受浏览器音频状态影响。

### 验收记录

- 用户确认文件夹与多选入口测试通过，50 张批量处理结果可完整下载；序列号清理复查通过。
- 用户确认文字写入、时间偏移测试通过；HEIC 部分文字字段按上述限制处理。
- 用户于 2026-09-07 确认 HEIC / JPG / PNG / TIF / WebP 混合图片地图选点写入 GPS，结果无差错。
- 发布收尾执行生产构建、静态检查及现有自动回归测试。用户实测记录不代表所有设备、容器变体均已覆盖。

### Git 留档与回退

- V2.0 基线：`v2.0-baseline-2026-09-06`。
- V2.1 正式版本：`v2.1.0`。
- 发布分支：`codex/v2.1-batch-alpha`（保留既有名称）。

可从标签创建独立工作目录，检查或运行历史版本，不覆盖当前工作区：

```bash
git worktree add --detach ../EXIF-Editor-v2.0 v2.0-baseline-2026-09-06
git worktree add --detach ../EXIF-Editor-v2.1 v2.1.0
```

两个目标目录须尚不存在；历史工作目录需要安装依赖后运行。Git 提交与标签保存在本地，远端推送和线上部署另行执行。
